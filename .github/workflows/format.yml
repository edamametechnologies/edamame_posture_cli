on:
  push:
    branches: [ dev, main ]
  pull_request:

# Auto cancel previous runs if they were not completed.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

name: Rust format

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest

    # CI-3 / SG-002: secrets are step-scoped, not job-scoped. The envcrypt
    # compile inputs live on the one step that compiles (`cargo hack check`);
    # fmt / install steps see no secrets. LLM API keys are not used by any
    # step this workflow runs and are deliberately absent.
    
    steps:

      - name: Setup EDAMAME Posture
        uses: edamametechnologies/edamame_posture_action@v1
        with:
          edamame_user: ${{ vars.EDAMAME_POSTURE_USER }}
          edamame_domain: ${{ vars.EDAMAME_POSTURE_DOMAIN }}
          edamame_pin: ${{ secrets.EDAMAME_POSTURE_PIN }}
          edamame_id: ${{ github.run_id }}
          checkout: true
          token: ${{ secrets.DEV_GITHUB_TOKEN }}
          wait_for_api: true

      - name: Configure Git Authentication to import private crates
        run: |
          git config --global url."https://edamamedev:${{ secrets.DEV_GITHUB_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - run: rustup component add rustfmt

      - name: Check code format
        run: |
          cargo fmt --all -- --check

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y pkg-config libgtk-3-dev protobuf-compiler libpcap-dev

      - name: Install cargo-hack
        run: |
          cargo install -f cargo-hack
  
      - name: Check all features
        # envcrypt consumes these at compile time (envc! fails the build when
        # one is missing), so they are needed to compile -- but only here.
        env:
          EDAMAME_APP_SENTRY: ${{ secrets.EDAMAME_APP_SENTRY }}
          EDAMAME_TARGET: ${{ secrets.EDAMAME_TARGET }}
          EDAMAME_CORE_SERVER: ${{ secrets.EDAMAME_CORE_SERVER }}
          EDAMAME_CA_PEM: ${{ secrets.EDAMAME_CA_PEM }}
          EDAMAME_CLIENT_PEM: ${{ secrets.EDAMAME_CLIENT_PEM }}
          EDAMAME_CLIENT_KEY: ${{ secrets.EDAMAME_CLIENT_KEY }}
          EDAMAME_SERVER_PEM: ${{ secrets.EDAMAME_SERVER_PEM }}
          EDAMAME_SERVER_KEY: ${{ secrets.EDAMAME_SERVER_KEY }}
          EDAMAME_CLIENT_CA_PEM: ${{ secrets.EDAMAME_CLIENT_CA_PEM }}
          LAMBDA_SIGNATURE: ${{ secrets.LAMBDA_SIGNATURE }}
          MIXPANEL_TOKEN: ${{ secrets.MIXPANEL_TOKEN }}
        run: |
          cargo hack check --all-features --all-targets